import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.demo.couchbaselite.hotels',
  appName: 'CBL Hotels',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
